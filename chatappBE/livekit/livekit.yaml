port: 7880

rtc:
  port_range_start: 50000
  port_range_end: 50500
  tcp_port: 7881
  # node_ip and keys are passed via --node-ip and --keys CLI flags in docker-compose
  # so that env var substitution is handled by Docker Compose, not LiveKit's YAML parser.

turn:
  enabled: true
  udp_port: 3478
  external_tls: false

webhook:
  urls:
    - http://voice-service:8091/internal/livekit/webhook
  api_key: chatapp

logging:
  level: info
