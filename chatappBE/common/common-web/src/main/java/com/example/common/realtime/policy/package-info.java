/**
 * Transitional location for realtime flow policy contracts.
 *
 * These types are intentionally retained here for backward compatibility with
 * non-common modules. Future refactors may relocate them behind compatibility aliases.
 */
package com.example.common.realtime.policy;
